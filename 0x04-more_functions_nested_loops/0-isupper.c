#include <stdio.h>
#include "main.h"

/**
 * _isupper - checks for uppercase character.
 *
 * @c: Character to be checked.
 *
 * Return: (1) Success, (0) Otherwise.
 */

int _isupper(int c)
{
	int i = isupper(c);
	return (i);
}
